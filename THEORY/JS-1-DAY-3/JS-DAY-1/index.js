let num_0f_elements=Number(prompt("How many elements are there?"));
let arr=[];
for(let i=0;i<num_0f_elements;i++)
    {
        let num=Number(prompt(`Enter the element`));
        arr.push(num);
    }
    console.log(arr);